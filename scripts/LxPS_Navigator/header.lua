local plugin = {}

plugin["name"] = "LxPS_Navigator"
plugin["author"] = "LexxersPS"
plugin["version"] = "1.0"
plugin["description"] = "Advanced navigation library providing A* pathfinding and navigation mesh management"

plugin["load"] = false  -- load it when true
plugin["is_library"] = true  -- optional if a lib
plugin["is_required_dependency"] = false -- optional if a lib

return plugin